CLI = Command Line Inteface

매뉴얼 조회 : man

파일 목록/내용 조회 관련 명령어 : ls, cat, head, tail
ls -al : 모든 파일 / 권한 보임
cat 검색어 + tab 2번 = 검색어로 시작하는 단어 뭐 있는지 보여줌
head -n 숫자 = 숫자만큼 상위 항목 출력
less = 페이지 단위로 로그 볼 수 있음

검색/탐색 관련 명령어 : grep, find

압축/해제 관련 명령어 : tar, gzip/gunzip, zip/unzip

시간 관련 명령어:date, cal

기타 명령어 : echo, exit, history

관리자 권한 실행 : sudo

패키지 매니저 : apt

텍스트 에디터 : nano
